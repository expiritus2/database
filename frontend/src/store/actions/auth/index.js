import { createAction } from 'redux-actions';

export const requestGetCurrentUserAction = createAction('REQUEST/GET_CURRENT_USER');
export const requestPostLoginAction = createAction('REQUEST/POST_LOGIN');
